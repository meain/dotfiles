#!/bin/sh

# Delete remote branchs

set -e

git branch --merged | egrep -v "(^\*|master|dev)" | xargs git branch -d
printf "Delete upstream ones?(y/N): "
[ "$REPLY" == "y" ] && git remote prune $(git remote | fzf --height 40% --reverse)
