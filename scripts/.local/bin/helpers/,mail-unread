#!/bin/sh

# Get unread emails

inbox="$(notmuch search tag:important and tag:unread and tag:inbox and '-tag:notified' |
    sed 's|\(^[^ ]*\) [^]]*\]|\1|' | sed 's/([^(]*)$//;s/^\ */- /')"
if echo "$inbox" | grep -q .; then
    echo "$inbox"
fi
