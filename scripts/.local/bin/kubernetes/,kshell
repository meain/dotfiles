#!/bin/sh

# Shell into a kubernetes pod

set -e

kubectl get pods --all-namespaces | tail -n +2 | fzf --query "$*" | awk '{print "-n",$1,$2}'  |
	xargs -I{} echo kubectl exec -it {} -- sh -c 'clear; (bash || ash || sh)'
