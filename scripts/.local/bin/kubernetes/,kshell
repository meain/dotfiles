#!/bin/sh

# Shell into a kubernetes pod

set -e

kubectl get pods | tail -n +2 | awk '{print $3,$1}' | fzf --query "$*" | cut -d' ' -f2 |
	xargs -I{} kubectl exec -it "{}" -- sh -c 'clear; (bash || ash || sh)'
