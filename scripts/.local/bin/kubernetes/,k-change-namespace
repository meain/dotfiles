#!/bin/sh

# Change default namespace in kubernetes config

set -eo pipefail

kubectl get namespace | tail -n+2 | ,picker --query "$1" | cut -d' ' -f1 |
    xargs -r -I{} kubectl config set-context --current --namespace="{}"
