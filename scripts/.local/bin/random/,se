#!/bin/sh

# se -> script execute

set -eo pipefail

find -L "$HOME/.local/bin" -type f | sed "s|$HOME/.local/bin/||" |
	dmenu -i -fn 'Anka/Coder:size=8' -nb '#000000' -nf '#aaaaaa' -sb '#263238' -sf '#ffffff' -l 13 -p "Run script" |
	xargs -I{} zsh -ic "$HOME/.local/bin/{}"
