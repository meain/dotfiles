#!/bin/sh

# se -> script execute

set -eo pipefail

script="$(find -L "$HOME/.local/bin" -type f | sed "s|$HOME/.local/bin/||" |
	,picker -p "Run script: ")"

zsh -ic "$HOME/.local/bin/$script" | xargs -0 -I{} notify-send "[DONE] $script" "{}"
