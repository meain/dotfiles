npmgit() {
    PACKAGE=$(cat package.json | jq '.dependencies' | grep -v '{' | grep -v '}' | sed 's:..\"::' | sed 's:\"\:.*$::' | fzf)
    GITHUB_LINK=$(npm info $PACKAGE --json | jq '.repository.url' | sed 's:^\"git\+::' | sed 's:\.git\"$::')
    echo $GITHUB_LINK
    open $GITHUB_LINK
}
