#!/bin/sh

alias vim='nvim'
alias ivm='vim'
alias vm='nvim'
alias vi='nvim'
alias v='nvim'

alias viml='nvim "+source /tmp/vim-prev"'

vv() {
    $EDITOR +OpenLastFile
}

vs() {
    nvim "scp://$1/"
}
