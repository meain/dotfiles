#!/bin/sh

alias eserver='emacs --bg-daemon'
alias ec='pgrep Emacs || emacs --bg-daemon;emacsclient --no-wait -c'
alias ekillserver='emacsclient -e "(kill-emacs)"'
alias erestart='emacsclient -e "(kill-emacs)";emacs --bg-daemon'
